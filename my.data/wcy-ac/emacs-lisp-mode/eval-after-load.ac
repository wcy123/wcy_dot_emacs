(nil "(eval-after-load \"" 
     (completing-read "library: "
                      'locate-file-completion
                      (cons load-path (get-load-suffixes)))
     "\"" > ?\n
     "'(progn " > ?\n > _ ?\n
     "))" > ?\n)


;; Local Variables:
;; mode:emacs-lisp
;; coding: utf-8-unix
;; End:
