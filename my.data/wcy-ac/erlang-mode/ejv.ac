(nil
 (ignore (setq a (read-string "Var name: ")))
"get_"  a "(Vars) ->
    ej_vars:get(" a ",?MODULE, Vars).
set_" a "(Value,Vars) ->
    ej_vars:set(" a ", Value, ?MODULE, Vars)."
  )

;; Local Variables:
;; mode:emacs-lisp
;; coding: utf-8-unix
;; End:
